[project]
name = "newsmap-app"
version = "0.1.0"
description = "Streamlit newsmap visualization"
requires-python = ">=3.9"
dependencies = [
  "streamlit>=1.28",
  "streamlit-plotly-events>=0.0.6",
  "pandas>=2.0",
  "plotly>=5.15",
  "requests>=2.30",
]

[tool.uv]
dev-dependencies = []
